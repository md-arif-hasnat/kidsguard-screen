package com.example.kidsguard.navigation

enum class Screen {
    RoleSelection, Home, Locked, Settings, ParentDashboard, SafeZoneList, ActivityFeed, ChildSetup, ParentSetup, LocationHistory, DeveloperMenu, LiveMap
}
